# Syntactic Rules

program
    : expr ( ';' expr )* ';'?

expr
    : expr ( REL_OP expr )+           # Relations
    : expr ( '+' | '-' ) expr         # Terms
    | expr ( '*' | '/' | '%' ) expr   # Factors
    | '-' expr                        # Negation
    | expr '^' expr                   # Exponents
    | '(' expr ')'                    # Parenthesis
    | IDENT                           # Identifiers
    | NUM | 'true' | 'false'          # Literals

# Lexical Rules

REL_OP  : '<' | '>' | '<=' | '>=' | '==' | '!='

IDENT   : '_'
NUM     : [0-9]+ ( '.' [0-9]+ )?
BLANKS  : [ \t\v\f\r\n]+ -> ignored
